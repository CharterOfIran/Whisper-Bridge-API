# 🌉 پل نجوا (Whisper-Bridge-API)

پل نجوا یا **Whisper-Bridge** یک زیرساخت فوق‌سبک، امن و کاملاً ناشناس برای دریافت بازخورد و ایده است. این پروژه با هدف ایجاد یک بستر ارتباطی بدون ردپا، بر پایه زبان Go و معماری Serverless توسعه یافته است.

---

## 🛠️ نقشه راه: از ارسال تا انتشار و چکش‌کاری

فرآیند ثبت و پردازش در این سیستم از یک پروتکل چندمرحله‌ای برای تضمین ۱۰۰٪ ناشناسی و شفافیت عبور می‌کند:

1.  **ارسال امن (Frontend):** پیام کاربر از طریق فرم وب ارسال می‌شود. در این مرحله فیلتر Honeypot ربات‌ها را شناسایی کرده و لایه اول Sanitization کاراکترهای مخرب را خنثی می‌کند.
2.  **دریافت و هش‌گذاری (API):** سرور بدون ذخیره IP، یک اثر انگشت یک‌طرفه (Hash) موقت برای کنترل نرخ ارسال می‌سازد تا از اسپم جلوگیری شود.
3.  **بایگانی در صندوق امانات (Private Vault):** پیام‌ها ابتدا در یک مخزن گیت‌هاب **خصوصی** ذخیره می‌شوند تا توسط تیم بازبینی از نظر امنیتی چک شوند (حذف احتمالی اسامی یا اطلاعات شخصی تصادفی).
4.  **انتشار دوگانه (Public Release):** پس از تایید اولیه، داده‌ها به دو صورت در دسترس عموم قرار می‌گیرند:
    * **نظرات خام (Raw Data):** پیام‌های اصلی (بدون دستکاری در محتوا) جهت حفظ امانت‌داری و امکان آدیت همگانی.
    * **نسخه‌های خلاصه‌شده (Summarized):** خروجی‌های ورژن‌بندی شده توسط هوش مصنوعی برای مطالعه سریع.
5.  **چکش‌کاری همگانی (The Forge):** اینجاست که مشارکت واقعی شکل می‌گیرد. ایده در معرض دید همه قرار دارد و افراد از هر صنفی — از **پزشک و مهندس تا نانوا و وکیل** — می‌توانند روی نظرات (چه خام و چه خلاصه‌شده) نقد بنویسند یا آن‌ها را بهبود دهند.
6.  **تصویب جمعی:** پس از پخته شدن ایده در فرآیند چکش‌کاری، خروجی نهایی به عنوان بخشی از منشور یا لیست وظایف ثبت می‌شود.

---

## 🔄 چرخه حیات ایده و پالایش جمعی

ما به دنبال «بهینه‌سازی» هستیم. هر مشارکت طبق این الگو پیش می‌رود:
* **تطبیق:** آیا این موضوع قبلاً مطرح شده؟ (جلوگیری از انباشت اطلاعات تکراری).
* **پالایش:** نظرات توسط داوطلبان و با کمک هوش‌های مصنوعی پیشرفته پردازش می‌شوند.
* **شفافیت:** کاربر همواره می‌تواند نسخه خلاصه‌شده را با نسخه خام تطبیق دهد تا از صحت فرآیند مطمئن شود.

> **نکته:** هر کسی، با هر سطح از دانش و تخصص، می‌تواند در مرحله چکش‌کاری شرکت کند. هدف ما رسیدن به منطقی‌ترین و عادلانه‌ترین خروجی ممکن از دل نظرات متفاوت است.
---

## 🛡️ سوگندنامه حریم خصوصی

* **ردپای صفر:** هیچ آدرس IP، مشخصات مرورگر یا کوکی ذخیره نمی‌شود.
* **شفافیت مطلق:** کدهای پردازشگر در این مخزن برای بازرسی همگانی در دسترس است.
* **بدون تحلیل‌گر:** هیچ سرویس ردیابی جانبی در این پروژه وجود ندارد.

---

## 🏗️ معماری و ساختار مهندسی

این پروژه بر اساس یک ساختار استاندارد و ماژولار Go طراحی شده است تا تفکیک کامل وظایف (Separation of Concerns) و امنیت حداکثری در فرآیندها تضمین شود:

* الف: **api/**: مدیریت درخواست‌های ورودی Vercel بر پایه پروتکل‌های Serverless.
*  ب: **internal/ratelimit**: پیاده‌سازی منطق جلوگیری از اسپم بر اساس اثرانگشت موقت (بدون ذخیره IP یا اطلاعات هویتی).
*  ج: **internal/security**: لایه اختصاصی خنثی‌سازی تگ‌های مخرب و پاک‌سازی متون (Sanitization).
*  د: **internal/github**: مدیریت تعاملات امن و انتقال داده به مخزن Private Vault.
*  ه: **web/**: رابط کاربری سبک، ایزوله و فاقد هرگونه ابزار ردیابی یا تحلیل‌گر (No-Tracker Policy).
